
/* define a function and return the result,
Then Log with two similar numbers*/
 /* and Log with different numbers*/
function computeSum(a, b) {
    if (a === b) {
      return 3 * (a + b);
    } else {
      return a + b;
    }
  }
  

  console.log(computeSum(3, 3));
  
  console.log(computeSum(2, 4));


  